server:
  port: 8004
eureka:
  instance:
    hostname: 127.0.0.1  #服务器的IP地址
  client:
    serviceUrl:    #注册服务器的地址
      defaultZone: http://127.0.0.1:8008/eureka/
spring:
  application:
    name: frontService
  servlet:
    multipart:
      enabled: true